workflow: GitHubFlow/v1
tag-prefix: '[vV]?'
branches:
  main:
    mode: ContinuousDeployment
    label: ''
    increment: Minor
  feature:
    label: 'alpha'
    increment: Inherit
  pull-request:
    label: 'pr'
    increment: Inherit
